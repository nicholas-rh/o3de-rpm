--- Gems/RecastNavigation/CMakeLists.txt 2023-06-14 14:55:51.006717272 -0400
+++ Gems/RecastNavigation/CMakeLists.txt 2023-06-14 14:56:14.772565769 -0400
@@ -6,12 +6,12 @@
 #
 #
 
-set(gem_path ${CMAKE_CURRENT_LIST_DIR})
-set(gem_json ${gem_path}/gem.json)
-o3de_restricted_path(${gem_json} gem_restricted_path gem_parent_relative_path)
+#set(gem_path ${CMAKE_CURRENT_LIST_DIR})
+#set(gem_json ${gem_path}/gem.json)
+#o3de_restricted_path(${gem_json} gem_restricted_path gem_parent_relative_path)
 
-o3de_pal_dir(pal_dir ${CMAKE_CURRENT_LIST_DIR}/Platform/${PAL_PLATFORM_NAME} "${gem_restricted_path}" "${gem_path}" "${gem_parent_relative_path}")
+#o3de_pal_dir(pal_dir ${CMAKE_CURRENT_LIST_DIR}/Platform/${PAL_PLATFORM_NAME} "${gem_restricted_path}" "${gem_path}" "${gem_parent_relative_path}")
 
-ly_add_external_target_path(${CMAKE_CURRENT_LIST_DIR}/3rdParty)
+#ly_add_external_target_path(${CMAKE_CURRENT_LIST_DIR}/3rdParty)
 
-add_subdirectory(Code)
+#add_subdirectory(Code)
